// The `generator` block specifies the Prisma client configuration.
generator client {
  provider = "prisma-client-js" // Specifies that the Prisma client should be generated using the Prisma JavaScript client.
}

// The `datasource` block defines the connection to the database.
datasource db {
  provider = "postgresql" // Specifies that the database provider is PostgreSQL.
  url      = env("DATABASE_URL") // Specifies the database URL, which is retrieved from the environment variable `DATABASE_URL`.
}

// The `model` block defines the `url` table in the database, with its structure.
model url {
  id            String   @id @default(cuid())  // Defines the `id` field as a primary key with a unique value generated by `cuid()`.
  originalUrl   String   // Defines the `originalUrl` field to store the original long URL.
  shortCode     String   @unique  // Defines the `shortCode` field to store the shortened URL, ensuring it is unique.
  createdat     DateTime @default(now()) // Defines the `createdat` field to store the creation date, defaulting to the current date and time.
  visits        Int      @default(0)  // Defines the `visits` field to track the number of visits, defaulting to 0.
}
